/*
let inputXlsx = document.getElementById('excel-input');

inputXlsx.addEventListener('change', async function() {
    json = await readXlsxFile(inputXlsx.files[0])
    person = json.map(([firstName, lastName]) => `${firstName} ${lastName}`);
});
*/